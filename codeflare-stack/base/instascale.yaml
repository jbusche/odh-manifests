apiVersion: codeflare.codeflare.dev/v1alpha1
kind: InstaScale
metadata:
  name: instascale
spec:
  maxScaleoutAllowed: 15
  controllerResources:
    limits:
      cpu: ${instascale_cpu_limit}
      memory: ${instascale_memory_limit}
    requests:
      cpu: ${instascale_cpu_request}
      memory: ${instascale_memory_request}
#      cpu: '1'
#      memory: 1G
#    requests:
#      cpu: '1'
#      memory: 1G

